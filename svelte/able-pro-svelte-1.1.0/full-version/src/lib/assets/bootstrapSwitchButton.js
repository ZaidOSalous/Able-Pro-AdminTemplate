import bootstrapSwitchButton from 'bootstrap-switch-button/dist/bootstrap-switch-button.min.js?client';

export default async function bootstrapswitchbutton(node, options) {
  new bootstrapSwitchButton(node);
}
